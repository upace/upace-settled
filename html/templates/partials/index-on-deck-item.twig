<a class="land-ondeck-item">
    <div class="ondecksequence">
        <div class="ondecktime">{{ startTime }}</div>
        <div class="ondeckdate">{{ date }}</div>
        <div class="ondeckgear">{{ name }}</div>
        <div class="ondeckroom">{{ roomName }}</div>
    </div>
</a>
